# Notebooks

Reserved for exploratory analysis and model prototyping (Phase 5 —
forecasting/backtesting). Empty for now; nothing in Phase 1 needs a notebook
since the analytics functions are already unit-tested directly in
`backend/tests/`.
